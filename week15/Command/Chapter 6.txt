# Git cloning
#git clone https://github.com/PacktPublishing/Mastering-ROS-for-Robotics-Programming-Third-edition.git
#cd Mastering-ROS-for-Robotics-Programming-Third-edition
#cp -R ./Chapter4/* ~/catkin_ws/src
#cp -R ./Chapter6/* ~/catkin_ws/src
#cd ~/catkin_ws/src

catkin_make

sudo apt-get install ros-noetic-moveit ros-noetic-moveit-plugins ros-noetic-moveit-planners

# setelah memuat (kembali) mastering_ros_robot_description_pkg

roslaunch moveit_setup_assistant setup_assistant.launch

# setelah membangun MoveIt! package

roslaunch seven_dof_arm_config demo.launch

# setelah memuat (kembali) seven_dof_arm_gazebo

sudo apt-get install ros-noetc-joint-state-controller ros-noetic-position-controllers ros-noetic-joint-trajectory-controller 

roslaunch seven_dof_arm_gazebo seven_dof_arm_bringup_moveit.launch  

# setelah memuat dan/atau membangun (kembali) diff_wheeled_robot_gazebo 

sudo apt-get install ros-noetic-navigation ros-noetic-gmapping ros-noetic-map-server

roslaunch diff_wheeled_robot_gazebo diff_wheeled_gazebo_full.launch

roslaunch diff_wheeled_robot_gazebo gmapping.launch

roslaunch diff_wheeled_robot_control keyboard_teleop.launch

rosrun map_server map_saver -f map1

# memulai ulang simulasi dengan automasi pergerakan

roslaunch diff_wheeled_robot_gazebo diff_wheeled_gazebo_full.launch

roslaunch diff_wheeled_robot_gazebo amcl.launch 